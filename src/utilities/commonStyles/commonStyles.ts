import {StyleSheet} from 'react-native';

import {colors, WP, HP, size, appFonts} from '../../utilities';

export const commonstyles: any = StyleSheet.create({
  aiRow: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  icon24: {
    height: 24,
    width: 24,
  },
});
